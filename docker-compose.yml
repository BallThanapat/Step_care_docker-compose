version: '3.8'

services:
  frontend:
    build:
      context: ./Frontend
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    environment:
      - VITE_API_URL=http://34.87.91.112:8000
    networks:
      - app-network
    depends_on:
      - backend

  backend:
    build:
      context: ./Backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - POSTGRES_DB=stepcare_db
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=bunnyz2710
      - DJANGO_SETTINGS_MODULE=studforce.settings
    volumes:
      - ./Backend:/app
    networks:
      - app-network

volumes:
  postgres_data:

networks:
  app-network:
    driver: bridge
